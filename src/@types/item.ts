export interface ItemData {
  content: string
  projectId: string
  dueString: string
}

export interface ItemDescription {
  createdBy?: string
  completedBy?: string
  messageId?: string
}
